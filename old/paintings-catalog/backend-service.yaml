apiVersion: v1
kind: Service
metadata:
  name: paintings-catalog-backend
  namespace: paintings-catalog
spec:
  type: ClusterIP
  selector:
    app: paintings-catalog-backend
  ports:
    - name: http
      port: 8080
      targetPort: 8080
