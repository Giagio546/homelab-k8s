apiVersion: v1
kind: Service
metadata:
  name: paperless-web
  namespace: paperless
spec:
  type: ClusterIP
  selector:
    app: paperless
  ports:
    - name: http
      port: 8000
      targetPort: 8000